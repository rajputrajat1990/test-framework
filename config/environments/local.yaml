# Local Environment Configuration
# Generated by quick-start script

environment:
  name: "local"
  description: "Local development and testing environment"
  
confluent_cloud:
  organization_id: "52445b30-3393-47c5-b174-9a856fc7ca26"
  environment_id: "env-33g8y2"
  cluster_id: "lkc-3pg922"
  region: "us-east-2"
  cloud: "aws"

testing:
  execution_mode: "apply"
  cleanup_policy: "always"
  resource_prefix: "local-test"
  timeout_minutes: 15
  
module_overrides:
  kafka_topic:
    parameters:
      partitions: 1
      topic_config:
        retention.ms: "86400000"  # 1 day
        
  s3_source_connector:
    enabled: false
    
  rbac_cluster_admin:
    parameters:
      principal: "User:rajat.rajput@koenig-solutions.com"

  rbac_topic_access:
    parameters:
      principal: "User:rajat.rajput@koenig-solutions.com"

validation:
  strict_mode: false
  skip_api_verification: false
  allow_partial_failures: true
  
logging:
  level: "INFO"
  include_sensitive: false
  log_file: "local-test.log"

limits:
  max_topics: 3
  max_connectors: 1
  max_role_bindings: 5
